export default function NextjsPage() {
    return (
        <main className="p-8">
            <h1 className="text-2xl font-bold mb-4">¿Qué es Next.js?</h1>
            <p className="text-lg leading-relaxed">
                Next.js es un framework basado en React que permite crear aplicaciones web modernas y optimizadas.
                Soporta renderizado del lado del servidor (SSR), generación estática (SSG), rutas automáticas,
                API routes, y muchas otras características que lo hacen ideal para aplicaciones de alto rendimiento.
            </p>
        </main>
    );
}
